// No modal for a plain render: the sheet only exists when a class tap was
// intercepted. Every other route renders this default and shows nothing.
export default function Default() {
  return null;
}
